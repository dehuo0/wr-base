This template adds oe-core's package-management feature to install package
management tools and preserve the package manager database on target.

To use, add

--with-template=feature/package-management

as a configure option, or

--enable-rootfs=glibc-(small|core)+package-management

Since package-management isn't part of glibc-small or
glibc-core by default;
